name: CD Backend

on:
  workflow_run:
    workflows: ["CI Backend"]
    types:
      - completed
    branches: [main]

jobs:
  deploy:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check if Dockerfile exists
        id: check_dockerfile
        run: |
          if [ -f "./backend/Dockerfile" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "⚠️ No Dockerfile found — skipping Docker build"
          fi

      - name: Login to DockerHub
        if: steps.check_dockerfile.outputs.exists == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push backend image
        if: steps.check_dockerfile.outputs.exists == 'true'
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: takouanaceur/medifolllow-backend:latest

      - name: Deployment summary
        run: |
          if [ "${{ steps.check_dockerfile.outputs.exists }}" == "true" ]; then
            echo "✅ Backend image pushed to DockerHub"
          else
            echo "⏭️ Docker build skipped — Dockerfile not found yet"
          fi
